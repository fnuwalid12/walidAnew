package SpecifierPractis;

public class Student {
// if we use final in the public class A, then you can not inheret from that class
	private int amount;
	
	public A() {
		amount = 0;
		
	}
	public void setAmount (int amount) {
		this.amount = amount; // since we used final when setting int, this will not be useable
		// bacause it is final now
// we used final in this method
		// we can only use one acess modifier 
		// we can have different specifier 
	}
}
